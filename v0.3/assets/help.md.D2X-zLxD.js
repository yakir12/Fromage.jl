import{_ as i,o as s,c as a,ao as t}from"./chunks/framework.DOId_bxO.js";const g=JSON.parse('{"title":"Help & troubleshooting","description":"","frontmatter":{},"headers":[],"relativePath":"help.md","filePath":"help.md","lastUpdated":null}'),n={name:"help.md"};function o(r,e,l,d,h,c){return s(),a("div",null,[...e[0]||(e[0]=[t(`<h1 id="Help-and-troubleshooting" tabindex="-1">Help &amp; troubleshooting <a class="header-anchor" href="#Help-and-troubleshooting" aria-label="Permalink to &quot;Help &amp; troubleshooting {#Help-and-troubleshooting}&quot;">​</a></h1><h2 id="It-printed-errors-and-stopped" tabindex="-1">&quot;It printed errors and stopped&quot; <a class="header-anchor" href="#It-printed-errors-and-stopped" aria-label="Permalink to &quot;&quot;It printed errors and stopped&quot; {#&quot;It-printed-errors-and-stopped&quot;}&quot;">​</a></h2><p>That&#39;s Fromage doing its job. Before anything is tracked, every row of both csv files is checked, and <strong>all</strong> problems are reported at once, one line per problematic row, e.g.:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>row 3: file does not exist, wrong start format</span></span></code></pre></div><p>The row&#39;s name is included too, so you can tell at a glance which entry needs fixing: issues with <code>rectifications.csv</code> show the row&#39;s <code>rectification_id</code> — e.g. <code>row 2 (rectification_id: morning): …</code> — and, if you gave your runs names (a <code>run_id</code> column in <code>runs.csv</code>), issues with runs show the <code>run_id</code> — e.g. <code>row 3 (run_id: beetle12): …</code>.</p><p>Nothing runs until they&#39;re all fixed — so nothing is ever half-done. Fix the mentioned rows in your csv files and run <code>main</code> again.</p><p>Among the things checked:</p><ul><li><p>all referenced files and folders exist, and the videos are actually readable;</p></li><li><p>timestamps are well-formatted, non-negative, ordered (<code>start</code> &lt; <code>stop</code>), and within the video&#39;s duration;</p></li><li><p>pixel coordinates lie inside the frame;</p></li><li><p>numeric parameters are within their valid ranges;</p></li><li><p><code>rectification_id</code>s are unique, and no two rectifications are effectively identical duplicates;</p></li><li><p>a filled cell in a column that the row&#39;s <code>type</code> doesn&#39;t use is flagged (it usually means the <code>type</code> itself is wrong);</p></li><li><p>the checkerboard is detected at the <code>extrinsic</code> timestamp, and — when a rectification window is given — at least 3 sampled frames within the intrinsic window [<code>intrinsic_start</code>, <code>intrinsic_stop</code>] have a detectable board (this is the expensive part of validation — it reads real frames);</p></li><li><p>for an <code>apriltag</code> rectification, at least <code>apriltags</code> tags of the chosen <code>family</code> are detected at the <code>extrinsic</code> frame and their metric fit converges;</p></li><li><p>segments of a multi-segment run agree on all their shared parameters;</p></li><li><p>segments of one run cut from the <strong>same</strong> video file are in order and do not overlap — each one&#39;s <code>start</code> at or after the previous one&#39;s <code>stop</code> (segments in <em>different</em> files cannot be compared, so their order is yours to get right);</p></li><li><p>every <code>rectification_id</code> used in <code>runs.csv</code> exists in <code>rectifications.csv</code>.</p></li></ul><h2 id="The-tracker-followed-the-wrong-thing" tabindex="-1">&quot;The tracker followed the wrong thing&quot; <a class="header-anchor" href="#The-tracker-followed-the-wrong-thing" aria-label="Permalink to &quot;&quot;The tracker followed the wrong thing&quot; {#&quot;The-tracker-followed-the-wrong-thing&quot;}&quot;">​</a></h2><p>You saw it in the <a href="/Fromage.jl/v0.3/results#The-diagnostic-video">diagnostic video</a> — good, that&#39;s what it&#39;s for. In rough order of likelihood: 2. <strong>Wrong <code>target_width</code>.</strong> Pause the run video, measure how many pixels wide the animal is, and put that in the <code>target_width</code> column. This is the single most important tracking parameter.</p><ol start="3"><li><p><strong>Wrong starting position.</strong> Give the run an explicit <code>start_location</code> (see <a href="/Fromage.jl/v0.3/runs#Where-the-tracker-starts-looking">where the tracker starts looking</a>).</p></li><li><p><strong>Wrong contrast direction.</strong> If your target is <em>lighter</em> than the background, set <code>darker_target</code> to <code>false</code>.</p></li><li><p><strong>The animal moves fast between frames.</strong> Increase <code>window_size</code>, or track at the video&#39;s full frame rate (don&#39;t lower <code>sample_fps</code>).</p></li></ol><h2 id="Iterating-faster:-run-only-part-of-the-pipeline" tabindex="-1">Iterating faster: run only part of the pipeline <a class="header-anchor" href="#Iterating-faster:-run-only-part-of-the-pipeline" aria-label="Permalink to &quot;Iterating faster: run only part of the pipeline {#Iterating-faster:-run-only-part-of-the-pipeline}&quot;">​</a></h2><p>While you&#39;re getting the csv files right, you don&#39;t have to run everything every time.</p><p>To check both csv files without processing anything, use <code>verify</code>:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">out </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Fromage</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">verify</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;path/to/data&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">out</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">rectifications      </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># rectifications.csv, annotated with an \`issues\` column</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">out</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">runs        </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># runs.csv, likewise</span></span></code></pre></div><p>It reports everything wrong with both files in one pass and never throws, so you can fix a whole dataset in one sitting rather than one error per run. Both tables come back whether or not anything was wrong — a dataset is accepted or rejected as a whole — so <code>all(isempty, out.rectifications.issues)</code> is the question to ask. Nothing is rectified or tracked. (<code>main</code> is the one that does the work; it aborts on any issue.)</p><p>Two more helpers run only one half of the pipeline (both still run the full validation of their csv file):</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># only build rectifications (all of them, or a subset of rectification_ids):</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Fromage</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">only_rectify</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;path/to/data&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; rectifications_file </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;rectifications.csv&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, rectification_ids </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;morning&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">])</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># only track (no rectification involved), optionally a subset of run_ids;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># writes one raw-view diagnostic per run: results_dir/1.mp4, 2.mp4, ...</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Fromage</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">only_track</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;path/to/data&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; runs_file </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;runs.csv&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, run_ids </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;run1&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;long&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">])</span></span></code></pre></div><p><code>main</code> itself also accepts <code>run_ids</code> to process only a subset of the runs (only the rectifications those runs reference are built). Every id you list must exist: if even one does not, the run stops with an error naming it and listing the ids that do exist, rather than quietly processing the ones that matched.</p><p><code>main</code> and <code>only_rectify</code> also accept <code>rectification_diagnostics = true</code>, which saves each rectification&#39;s warped extrinsic frame to <code>results_dir/rectifications/</code> so you can check a rectification before tracking against it — see <a href="/Fromage.jl/v0.3/results#The-rectification-images">the rectification images</a>.</p><h2 id="Re-running-in-the-same-Julia-session" tabindex="-1">Re-running in the same Julia session <a class="header-anchor" href="#Re-running-in-the-same-Julia-session" aria-label="Permalink to &quot;Re-running in the same Julia session {#Re-running-in-the-same-Julia-session}&quot;">​</a></h2><p>Fixing a csv file usually takes a few passes: run <code>main</code>, read what it says, edit a row, run it again. Only the first pass pays for the reading. Everything Fromage reads or detects while checking your files — one <code>ffprobe</code> per video, one read per MATLAB calibration file, the checkerboard or AprilTag detection at each rectification&#39;s <code>extrinsic</code> timestamp, and the scan of each intrinsic window — is remembered for as long as that Julia session is alive, so the rows you did not touch cost nothing the second time. Edit a row and only what that row changed is re-read.</p><p>This matters most where it hurts most: on a network share, reading a video is the slow part of checking a dataset, and a second run over a 300-run folder used to cost the same as the first.</p><p>There is one assumption in it, and it is worth knowing:</p><div class="warning custom-block"><p class="custom-block-title">A file is remembered by its name, not by its contents</p><p>Fromage never re-checks a file it has already read. If you <strong>replace a video or a <code>.mat</code> file in place</strong> — re-copying a corrupt recording, re-exporting a calibration — while Julia is still running, Fromage will keep reporting what the old file said. So will <code>Revise.jl</code> users who change Fromage itself mid-session.</p><p>Two ways out, either is fine:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Fromage</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">empty_caches!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># forget everything read so far; the next run reads it all again</span></span></code></pre></div><p>or simply quit Julia and start again. Renaming the new file instead of overwriting the old one also works, since the name is what is remembered.</p></div><p>What is remembered is what Fromage <em>found</em> — the video&#39;s size and duration, whether the board was detectable. A file it could <strong>not read at all</strong> is never remembered, so a network hiccup on the share does not become permanent: run it again and that file is read again.</p><p>Nothing else is affected either: the report you get is exactly the report you would have got from a cold start, and the <a href="/Fromage.jl/v0.3/results#The-issues-folder">issues folder</a> still gets a fresh, time-stamped folder on every run.</p><h2 id="Changing-a-default-for-all-rows-at-once" tabindex="-1">Changing a default for all rows at once <a class="header-anchor" href="#Changing-a-default-for-all-rows-at-once" aria-label="Permalink to &quot;Changing a default for all rows at once {#Changing-a-default-for-all-rows-at-once}&quot;">​</a></h2><p>The default of every <em>tuning</em> column can be overridden globally from <code>main</code>, so you don&#39;t have to fill in the same value on every row. The hierarchy is: a csv cell always wins over a global default, which wins over the built-in default (including the value probed from the video, for <code>yadif</code> and <code>native_fps</code>):</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">main</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;path/to/data&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">     rectification_defaults </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (n_corners </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">5</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">8</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">), blur </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">     tracking_defaults      </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (target_width </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 60</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, sample_fps </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 25</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">))</span></span></code></pre></div><ul><li><p><code>rectification_defaults</code> may set: <code>checker_width</code>, <code>n_corners</code>, <code>temporal_step</code>, <code>radial_parameters</code>, <code>blur</code>, <code>yadif</code>, and — for <code>type = apriltag</code> rows — <code>apriltags</code>, <code>family</code>, <code>tag_cell_width</code>.</p></li><li><p><code>tracking_defaults</code> may set: <code>target_width</code>, <code>window_size</code>, <code>darker_target</code>, <code>native_fps</code>, <code>sample_fps</code>, <code>initial_search_factor</code>, <code>downscale</code>, <code>background_length</code>.</p></li></ul><p>Anything else (identities, file names, timestamps, <code>start_location</code>/<code>center</code>/<code>north</code>) is per-row only, and an unrecognized or unconvertible entry is rejected with an error before anything runs. Global values pass through the same validation as csv cells — e.g. a global <code>sample_fps</code> must still not exceed each run&#39;s <code>native_fps</code>. <code>only_rectify</code> and <code>only_track</code> accept their respective keyword (<code>rectification_defaults</code> / <code>tracking_defaults</code>).</p><h2 id="Macs" tabindex="-1">Macs <a class="header-anchor" href="#Macs" aria-label="Permalink to &quot;Macs {#Macs}&quot;">​</a></h2><p>What matters is the CPU, not the macOS version (as of July 2026):</p><ul><li><p><strong>Intel Macs</strong> (<code>x86_64</code>): everything works — the full test suite runs on every commit on an Intel macOS runner.</p></li><li><p><strong>Apple Silicon Macs</strong> (M1/M2/M3/…, <code>aarch64</code>) running the native arm64 Julia: everything works <strong>except the AprilTag functionality</strong> — <code>type = apriltag</code> rectifications and drone tracking fail with <code>UndefVarError: libapriltag not defined</code>. The cause is upstream: <code>AprilTags_jll</code> ships no <code>aarch64-apple-darwin</code> binaries, so the AprilTag C library can never load. <code>checkerboard</code>, <code>uniform</code>, and <code>matlab</code> rectifications, and all tracking of ordinary (fixed-camera) runs, are unaffected.</p></li><li><p><strong>Workaround on Apple Silicon</strong>: install the <strong>Intel (x86_64) Julia binary</strong> and run it under Rosetta 2 — Julia then pulls the <code>x86_64-apple-darwin</code> artifacts for all binary dependencies, AprilTags included. Slower, but functional.</p></li></ul><p>Linux and Windows (x64) are fully tested in CI.</p><h2 id="Still-stuck?" tabindex="-1">Still stuck? <a class="header-anchor" href="#Still-stuck?" aria-label="Permalink to &quot;Still stuck? {#Still-stuck?}&quot;">​</a></h2><p>Ask in the lab, or <a href="https://github.com/yakir12/Fromage.jl/issues" target="_blank" rel="noreferrer">open an issue on GitHub</a> — include the exact error message and, if possible, the csv row that triggers it.</p>`,38)])])}const k=i(n,[["render",o]]);export{g as __pageData,k as default};
