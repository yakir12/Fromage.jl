import{_ as t,o as i,c as a,ao as o}from"./chunks/framework.UpDNKKgg.js";const s="/Fromage.jl/dev/assets/tracking-run.P_0kPd7f.webp",r="/Fromage.jl/dev/assets/diagnostic-dappled.DRrX4ODD.png",g=JSON.parse('{"title":"Your results","description":"","frontmatter":{},"headers":[],"relativePath":"results.md","filePath":"results.md","lastUpdated":null}'),n={name:"results.md"};function d(c,e,h,l,p,u){return i(),a("div",null,[...e[0]||(e[0]=[o('<h1 id="Your-results" tabindex="-1">Your results <a class="header-anchor" href="#Your-results" aria-label="Permalink to &quot;Your results {#Your-results}&quot;">​</a></h1><p>When <code>main</code> finishes, everything it produced is on disk, in a <code>results_dir</code> folder created in the folder Julia was started in: a track file per run, one diagnostic video, and — if you asked for them — an image per rectification.</p><h2 id="Choosing-the-output-folder" tabindex="-1">Choosing the output folder <a class="header-anchor" href="#Choosing-the-output-folder" aria-label="Permalink to &quot;Choosing the output folder {#Choosing-the-output-folder}&quot;">​</a></h2><p>To put the results somewhere else, name the folder with <code>results_dir</code>:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">main</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;path/to/data&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; results_dir </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;/scratch/experiment3&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>That folder <em>is</em> the output folder — the diagnostic video lands at <code>/scratch/experiment3/diagnostic.mp4</code>, not in a <code>results_dir</code> inside it — and it is created if it doesn&#39;t exist. Every path on this page that starts with <code>results_dir/</code> then starts with your folder instead. A relative name, like <code>results_dir = &quot;second_try&quot;</code>, is taken relative to the folder Julia is in when you call <code>main</code>.</p><p>Running <code>main</code> again into the same folder overwrites the track files and the diagnostic video, so naming a different folder is how you keep two analyses of the same data side by side. <code>verify</code> takes the same keyword, for where it saves <a href="/Fromage.jl/dev/results#The-issues-folder">the issues folder</a>.</p><h2 id="The-track-files" tabindex="-1">The track files <a class="header-anchor" href="#The-track-files" aria-label="Permalink to &quot;The track files {#The-track-files}&quot;">​</a></h2><p>Each run&#39;s track is written to <code>results_dir/&lt;run_id&gt;.csv</code> — a plain csv you can open in Excel, R, Python, MATLAB, or anything else. It has three columns:</p><table tabindex="0"><thead><tr><th style="text-align:right;">column</th><th style="text-align:right;">content</th></tr></thead><tbody><tr><td style="text-align:right;"><code>time</code></td><td style="text-align:right;">the timestamp of each detected coordinate, in seconds on the run&#39;s clock — which starts at the run&#39;s first <code>start</code> and advances one sampling interval per coordinate. For a run tracked from a single row, that is simply seconds into the video.</td></tr><tr><td style="text-align:right;"><code>x</code>, <code>y</code></td><td style="text-align:right;">the target&#39;s <strong>real-world</strong> coordinates at that time.</td></tr></tbody></table><div class="tip custom-block"><p class="custom-block-title"><code>time</code> when a run has several segments</p><p>The clock counts tracked frames, so time you left out of the run does not appear in it: a stretch cut out between two <a href="/Fromage.jl/dev/runs#Runs-made-of-several-segments">segments</a> of one file, and the join between two files, are both closed up rather than shown as a jump. Speeds computed from the track therefore ignore them, which is the intent. The real times are still in <code>runs.csv</code> if you need to put them back.</p></div><p>The coordinates are already fully converted — lens distortion, perspective, and scale are all corrected:</p><ul><li><p>The <strong>origin</strong> (0, 0) is at the rectification&#39;s <code>center</code> (if you gave one).</p></li><li><p>If you gave a <code>north</code> point, the coordinates are rotated so north is consistent across rectifications.</p></li><li><p>The <strong>unit</strong> is whatever your calibration used: the <code>checker_width</code> unit for checkerboard rectifications (e.g. cm if you measured your squares in cm), the <code>tag_cell_width</code> unit for AprilTag ones, the <code>pixel_width</code> unit for <code>uniform</code> ones, or the MATLAB calibration&#39;s unit for <code>matlab</code>.</p></li><li><p><code>x</code> grows rightward and <code>y</code> grows <strong>downward</strong> in the image, like the pixel convention.</p></li></ul><h2 id="The-diagnostic-video" tabindex="-1">The diagnostic video <a class="header-anchor" href="#The-diagnostic-video" aria-label="Permalink to &quot;The diagnostic video {#The-diagnostic-video}&quot;">​</a></h2><p><code>main</code> also writes <code>results_dir/diagnostic.mp4</code>: every run rendered top-down through its rectification into a fixed 540×540 canvas, with a circle around the tracked position, a trailing trace, and a label — one run after the other, playing at 2× real time (≈24 fps regardless of the run&#39;s <code>sample_fps</code>).</p><p>The label, top left, has two lines: the run&#39;s <code>run_id</code>, and beneath it <code>&lt;segment&gt; - &lt;time&gt;</code>, e.g. <code>2 - 00:02:14.123</code>. The segment is which of the run&#39;s <code>runs.csv</code> rows the frame comes from (1 for the first of them, 2 for the second, …), and the time is where the frame sits <strong>in that segment&#39;s own video file</strong> — open the file in any video player and seek to that time to find the same frame. It is not the <code>time</code> column of the track csv: that one is the run&#39;s continuous clock, and the two differ from the second segment on.</p><p>The canvas is oriented by the rectification&#39;s <code>north</code>, pointing up. Give two rectifications of the same arena a <code>center</code> and a <code>north</code> on the same physical landmarks and their tracks come out in the same orientation, so you can compare them directly. Without a <code>north</code>, each rectification is oriented however its own rectification frame happened to fall — for <code>apriltag</code> rectifications that means the orientation of one tag board, which is easy to change between field days without noticing.</p><p>This is what a healthy run looks like — the circle sits on the animal for the whole run, and the trace grows behind it from the centre of the arena to the edge (one complete run, looping):</p><p><img src="'+s+'" alt=""></p><p>And the same check works in harder conditions. Here the arena is covered in dappled light, yet the circle should still follow the animal — if it jumps to a bright patch instead, you&#39;ve caught a bad track:</p><p><img src="'+r+'" alt=""></p><div class="danger custom-block"><p class="custom-block-title">Watch it!</p><p>The diagnostic video is the fastest way to catch a tracker that latched onto a shadow, a wrong starting position, or a bad rectification. Watch it before analysing any tracks.</p></div><p>Things to look for:</p><ul><li><p>The circle should stay on your animal for the whole run — not jump to a shadow, a droppings mark, or a cable.</p></li><li><p>The arena should look right in the top-down view: straight edges straight, circles circular. A warped arena means a bad rectification.</p></li><li><p>The trace should look like a plausible path for your animal.</p></li></ul><h2 id="The-rectification-images" tabindex="-1">The rectification images <a class="header-anchor" href="#The-rectification-images" aria-label="Permalink to &quot;The rectification images {#The-rectification-images}&quot;">​</a></h2><p>Off by default. Ask for them and <code>main</code> saves, for every rectification, that rectification&#39;s <code>extrinsic</code> frame warped through the rectification fit to it:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">main</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;path/to/data&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; rectification_diagnostics </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>One JPEG per rectification lands in <code>results_dir/rectifications/</code>, named by its <code>rectification_id</code> — so <code>c1.jpg</code> is the rectification the csv calls <code>c1</code>.</p><p>That is true of a re-run too: Fromage remembers the rectifications it has already built (see <a href="/Fromage.jl/dev/help#Re-running-in-the-same-Julia-session">re-running in the same Julia session</a>), but it still saves every image again, from the rectification it remembered.</p><p>This is the same &quot;is the arena square?&quot; check the diagnostic video gives you, except you get it as soon as the rectifications are built, before a single run has been tracked. Straight arena edges should come out straight and circles circular. A bowed, sheared or wildly stretched image means the rectification is wrong, and there is no point tracking anything against it — fix the rectification first.</p><div class="tip custom-block"><p class="custom-block-title">AprilTag rectifications produce no image here</p><p>An <code>apriltag</code> rectification has no single fixed image-to-real map to warp through, because the drone moves and every frame is registered separately. Its top-down view is the per-run <a href="/Fromage.jl/dev/results#The-diagnostic-video">diagnostic video</a> instead.</p></div><h2 id="The-issues-folder" tabindex="-1">The issues folder <a class="header-anchor" href="#The-issues-folder" aria-label="Permalink to &quot;The issues folder {#The-issues-folder}&quot;">​</a></h2><p>If a rectification fails detection — the checkerboard or the AprilTags can&#39;t be found in its extrinsic frame — Fromage saves that exact frame so you can see what it saw. The message in the report tells you where it went, e.g.:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>row 2 (rectification_id: morning): only 4 of 6 AprilTags detected at the extrinsic frame — saved the extrinsic frame of /data/camera_a/board.mp4 at 1.0 s to /home/me/experiment/results_dir/issues/2026-08-20T14-22-05/board_t1.0s_3f9a02c1.png for inspection</span></span></code></pre></div><p>The frame is named for its video and extrinsic, followed by a short tag that tells apart videos with the same name in different folders — and two rectifications that looked at the same frame with different settings. The tag depends only on what was checked, so re-running on the same data saves the frame under the same name.</p><p>Each time you run Fromage it gets its own time-stamped folder under <code>results_dir/issues</code>, named for the moment it started, so the folder holds exactly the frames of that run of Fromage and older ones stay where they are. That is true of a re-run too: Fromage remembers what it detected (see <a href="/Fromage.jl/dev/help#Re-running-in-the-same-Julia-session">re-running in the same Julia session</a>), but it still saves the frame again, into the new folder. Nothing here is ever deleted: the folder is yours to clean out whenever you like.</p><p>Open the frame and look at it — a blurry, over-exposed, or half-out-of-shot board is usually the whole story, and the fix is a different <code>extrinsic</code> timestamp or a better rectification video.</p>',37)])])}const m=t(n,[["render",d]]);export{g as __pageData,m as default};
